# assets/

This directory will contain static assets for the web portal:

- `images/`   — Logos, icons, and UI imagery
- `fonts/`    — Custom web fonts (if not loaded from CDN)
- `icons/`    — SVG/icon files

**Phase:** Architecture setup — no assets created yet.
