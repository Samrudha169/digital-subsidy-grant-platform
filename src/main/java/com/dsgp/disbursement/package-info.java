/**
 * Package: com.dsgp.disbursement
 *
 * <p>Disbursement module — handles staged, milestone-based fund release,
 * scheduled payments, and disbursement status tracking.
 *
 * <p>Sub-packages:
 * <ul>
 *   <li>{@code controller} — REST API endpoints</li>
 *   <li>{@code service}    — Business logic / scheduler integration</li>
 *   <li>{@code repository} — Data access (Spring Data JPA)</li>
 *   <li>{@code entity}     — JPA entities</li>
 *   <li>{@code dto}        — Data transfer objects</li>
 * </ul>
 *
 * <p><strong>Phase:</strong> Architecture setup — no implementation yet.
 */
package com.dsgp.disbursement;
