/**
 * Package: com.dsgp.audit
 *
 * <p>Audit module — captures audit logs for user activity, security events,
 * approval/rejection history, and important system actions.
 *
 * <p>Sub-packages:
 * <ul>
 *   <li>{@code controller} — REST API endpoints for audit trail retrieval</li>
 *   <li>{@code service}    — Audit logging service</li>
 *   <li>{@code repository} — Data access (Spring Data JPA)</li>
 *   <li>{@code entity}     — JPA entities (AuditLog)</li>
 *   <li>{@code dto}        — Data transfer objects</li>
 * </ul>
 *
 * <p><strong>Phase:</strong> Architecture setup — no implementation yet.
 */
package com.dsgp.audit;
