/**
 * Package: com.dsgp.analytics
 *
 * <p>Analytics module — provides scheme-wise, region-wise dashboards,
 * fund utilization reports, disbursement reports, and non-compliance reports.
 *
 * <p>Sub-packages:
 * <ul>
 *   <li>{@code controller} — Dashboard and report API endpoints</li>
 *   <li>{@code service}    — Aggregation and reporting logic</li>
 *   <li>{@code dto}        — Data transfer objects for reports</li>
 * </ul>
 *
 * <p>No repository/entity packages required initially; analytics queries
 * will be served through cross-module service calls or read-only queries.
 *
 * <p><strong>Phase:</strong> Architecture setup — no implementation yet.
 */
package com.dsgp.analytics;
